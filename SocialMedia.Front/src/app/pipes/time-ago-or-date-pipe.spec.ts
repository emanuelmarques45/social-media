import { TimeAgoOrDatePipe } from './time-ago-or-date-pipe';

describe('TimeAgoOrDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoOrDatePipe();
    expect(pipe).toBeTruthy();
  });
});
